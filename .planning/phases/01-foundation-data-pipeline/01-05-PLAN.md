---
phase: 01-foundation-data-pipeline
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/prisma/schema.prisma
autonomous: true
gap_closure: true

must_haves:
  truths:
    - 'GraphNode upsert operations succeed with childId + normalizedLabel constraint'
    - 'BullMQ job processes watch history without runtime errors'
  artifacts:
    - path: 'apps/web/prisma/schema.prisma'
      provides: 'GraphNode model with unique constraint'
      contains: '@@unique([childId, normalizedLabel])'
  key_links:
    - from: 'apps/web/src/lib/graph/builder.ts:73'
      to: 'apps/web/prisma/schema.prisma'
      via: 'Prisma upsert childId_normalizedLabel'
      pattern: 'childId_normalizedLabel'
---

<objective>
Add missing unique constraint to GraphNode model to enable upsert operations in builder.ts.

Purpose: The builder service uses Prisma's upsert with `where: { childId_normalizedLabel }` but the schema only has an index, not a unique constraint. This causes runtime PrismaClientKnownRequestError when graph build jobs execute.

Output: Updated schema.prisma with @@unique constraint and successful database migration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-foundation-data-pipeline/01-VERIFICATION.md
@apps/web/prisma/schema.prisma
@apps/web/src/lib/graph/builder.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add unique constraint to GraphNode schema</name>
  <files>apps/web/prisma/schema.prisma</files>
  <action>
Update the GraphNode model in `apps/web/prisma/schema.prisma` to change the index to a unique constraint.

Find this line (around line 561):

```prisma
@@index([childId, normalizedLabel])
```

Replace with:

```prisma
@@unique([childId, normalizedLabel])
```

This enables Prisma's upsert operation to use `childId_normalizedLabel` as the where clause identifier, which the builder.ts relies on at line 73.

Keep the other indexes unchanged:

- `@@index([childId])` - for child-scoped queries
- `@@index([childId, category])` - for category filtering
- `@@index([childId, totalWatchTime])` - for ranking
  </action>
  <verify>

1. File contains `@@unique([childId, normalizedLabel])` in GraphNode model
2. File still contains other indexes (`@@index([childId])`, `@@index([childId, category])`, etc.)
3. `npx prisma validate` passes (schema is valid)
   </verify>
   <done>
   GraphNode model has @@unique([childId, normalizedLabel]) constraint.
   </done>
   </task>

<task type="auto">
  <name>Task 2: Run database migration</name>
  <files>apps/web/prisma/migrations/</files>
  <action>
Run Prisma migration to apply the unique constraint to the database:

```bash
cd apps/web && npx prisma migrate dev --name add_graph_node_unique_constraint
```

This will:

1. Generate migration SQL file adding unique constraint
2. Apply migration to development database
3. Regenerate Prisma client with updated types

If the migration fails due to duplicate data (unlikely in dev), you may need to:

```bash
cd apps/web && npx prisma migrate reset --force
```

After migration, generate client:

```bash
cd apps/web && npx prisma generate
```

  </action>
  <verify>
1. New migration file exists in `apps/web/prisma/migrations/` with "add_graph_node_unique_constraint" in name
2. `npx prisma migrate status` shows no pending migrations
3. `npx prisma validate` passes
  </verify>
  <done>
Database has unique constraint on GraphNode(childId, normalizedLabel).
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify builder upsert compiles</name>
  <files>apps/web/src/lib/graph/builder.ts</files>
  <action>
Run TypeScript compilation to verify builder.ts upsert operations now have valid types:

```bash
cd apps/web && npx tsc --noEmit
```

The upsert at line 73 in builder.ts:

```typescript
await prisma.graphNode.upsert({
  where: {
    childId_normalizedLabel: {
      childId,
      normalizedLabel,
    },
  },
  // ...
});
```

Should now compile without errors because Prisma recognizes `childId_normalizedLabel` as a valid unique identifier from the `@@unique([childId, normalizedLabel])` constraint.

If there are type errors related to the constraint, run:

```bash
cd apps/web && npx prisma generate
```

Then retry TypeScript check.
</action>
<verify>

1. `cd apps/web && npx tsc --noEmit` passes with no errors
2. No errors referencing `childId_normalizedLabel` or upsert operations
   </verify>
   <done>
   TypeScript compilation passes, builder.ts upsert operations are type-safe.
   </done>
   </task>

</tasks>

<verification>
1. Schema validation: `cd apps/web && npx prisma validate` passes
2. Migration applied: `cd apps/web && npx prisma migrate status` shows no pending
3. TypeScript: `cd apps/web && npx tsc --noEmit` passes
4. Visual check: schema.prisma contains `@@unique([childId, normalizedLabel])` in GraphNode model
</verification>

<success_criteria>

- GraphNode model has @@unique([childId, normalizedLabel]) constraint
- Database migration applied successfully
- TypeScript compilation passes
- builder.ts upsert operations are valid
- Truth "BullMQ job processes watch history asynchronously" can now be verified (upsert won't throw)
  </success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-data-pipeline/01-05-SUMMARY.md`
</output>
